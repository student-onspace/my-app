---
apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
  labels:
    app: web
spec:
  containers:
    - name: front-end
      image: devopsonspace/java-app:v1.0
      ports:
        - containerPort: 80
  imagePullSecrets:
  - name: regcred
